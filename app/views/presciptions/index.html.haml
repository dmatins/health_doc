%h1 Listing presciptions

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Patient
      %th Physician
      %th Medication
      %th Quantity
      %th Issue date
      %th Expiration date
      %th Renew count
      %th Called in
      %th
      %th
      %th

  %tbody
    - @presciptions.each do |presciption|
      %tr
        %td= presciption.patient_id
        %td= presciption.physician_id
        %td= presciption.medication
        %td= presciption.quantity
        %td= presciption.issue_date
        %td= presciption.expiration_date
        %td= presciption.renew_count
        %td= presciption.called_in
        %td= link_to 'Show', presciption
        %td= link_to 'Edit', edit_presciption_path(presciption)
        %td= link_to 'Destroy', presciption, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Presciption', new_presciption_path
